export const SUCCESS_ACTION = (action: string, ref: string) =>
  `generate a new ${action} successfully , refrance id ${ref}`;
export const DELETE_ACTION = (action: string) =>
  `Delete ${action} successfully `;

export const SUPMIT_OPPORTUNITY_EMP = (ref: string) =>
  `Congratulations ! , You have a new opportunity submited from your EMP link : ${ref} `;
